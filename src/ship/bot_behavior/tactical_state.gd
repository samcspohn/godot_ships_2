class_name TacticalState
extends RefCounted
## Stub retained for forward compatibility.
## Bot decisions now use continuous threat scores (0–1 floats) and absolute
## distance thresholds rather than a state machine.
## See BotBehavior.get_threat_score() and each ship class's get_nav_intent().
